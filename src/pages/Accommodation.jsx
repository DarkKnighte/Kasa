import React from 'react';
// import { useParams } from 'react-router-dom'; // Si vous avez besoin de récupérer l'ID de l'URL

const Accommodation = () => {
    // const { id } = useParams(); // Exemple pour récupérer l'ID
    // Logique pour charger les données de l'hébergement en fonction de l'ID

    return (
        <div className="accommodation-container">
            {/* Le contenu détaillé de la page hébergement ira ici */}
            <p>Page Hébergement</p>
        </div>
    );
};

export default Accommodation;
